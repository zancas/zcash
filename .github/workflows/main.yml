name: CI

on: [pull_request]

jobs:
  linux_x64_build:
    runs-on: ubuntu-16.04
    steps:
     - name: checkout
       uses: actions/checkout@master
     - name: look and see
       run: pwd && ls -la
     - name: build
       uses: ./.github/build
